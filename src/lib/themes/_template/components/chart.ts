/**
 * Chart Component Theme Config
 *
 * Data visualization charts.
 *
 * Slots:
 * - base: Root container
 * - container: Chart container
 * - tooltip: Tooltip container
 * - tooltipContent: Tooltip content area
 */
export const chartConfig = {
  slots: {
    base: '',
    container: '',
    tooltip: '',
    tooltipContent: '',
  },
};
