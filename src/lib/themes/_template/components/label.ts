/**
 * Label Component Theme Config
 *
 * Form field label.
 *
 * Slots:
 * - base: Root <label> element styles
 */
export const labelConfig = {
  slots: {
    /**
     * base: Root label element
     *
     * Common styles:
     * - Font size (text-sm)
     * - Font weight (font-medium)
     * - Text color (text-foreground)
     * - Cursor (cursor-default for non-clickable)
     */
    base: [
      // TODO: Add your label styles
      // Example: 'text-sm', 'font-medium'
    ],
  },
};
