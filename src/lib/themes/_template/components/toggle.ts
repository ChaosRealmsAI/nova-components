/**
 * Toggle Component Theme Config
 *
 * Pressable button that toggles between on/off states.
 *
 * Slots:
 * - base: Root toggle button
 */
export const toggleConfig = {
  slots: {
    /**
     * base: Root toggle element
     *
     * Common styles:
     * - Border radius (rounded-md, rounded-lg)
     * - On state: data-[state=on]:bg-primary
     * - Hover (hover:bg-surface-1)
     */
    base: [
      // TODO: Add your toggle styles
      // Example: 'rounded-lg'
      // Example: 'data-[state=on]:bg-primary data-[state=on]:text-primary-foreground'
    ],
  },
};
