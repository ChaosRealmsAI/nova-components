/**
 * InputGroup Component Theme Config
 *
 * Input with attached addons (prefix/suffix).
 *
 * Slots:
 * - base: Root container
 * - addon: Prefix/suffix addon
 * - input: Input element
 */
export const inputGroupConfig = {
  slots: {
    base: '',
    addon: '',
    input: '',
  },
};
