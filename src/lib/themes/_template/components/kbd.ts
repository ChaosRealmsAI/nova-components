/**
 * Kbd Component Theme Config
 *
 * Keyboard key indicator (e.g., ⌘ + K).
 *
 * Slots:
 * - base: Key container
 */
export const kbdConfig = {
  slots: {
    /**
     * base: Keyboard key element
     *
     * Common styles:
     * - Border radius (rounded-md, rounded-sm)
     * - Font (font-mono)
     * - Background (bg-surface-2)
     * - Border (border, border-border)
     * - Shadow (shadow-sm) for 3D effect
     */
    base: [
      // TODO: 'rounded-md', 'font-mono'
      // TODO: 'bg-surface-2', 'border-border'
    ],
  },
};
