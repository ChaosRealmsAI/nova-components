/**
 * Textarea Component Theme Config
 *
 * Multi-line text input field.
 *
 * Slots:
 * - base: Root <textarea> element styles
 */
export const textareaConfig = {
  slots: {
    /**
     * base: Root textarea element
     *
     * Common styles:
     * - Same as Input (background, border, focus states)
     * - Min height (min-h-[80px])
     * - Resize behavior (resize-none, resize-y)
     */
    base: [
      // TODO: Add your textarea styles
      // Usually similar to inputConfig.slots.base
    ],
  },
};
