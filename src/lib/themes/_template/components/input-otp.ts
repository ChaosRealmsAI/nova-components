/**
 * InputOTP Component Theme Config
 *
 * One-time password input (verification codes).
 *
 * Slots:
 * - container: Root container
 * - group: Group of input slots
 * - slot: Individual digit input
 * - separator: Separator between groups
 */
export const inputOtpConfig = {
  slots: {
    container: '',
    group: '',
    slot: '',
    separator: '',
  },
};
