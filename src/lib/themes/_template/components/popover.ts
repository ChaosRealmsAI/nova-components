/**
 * Popover Component Theme Config
 *
 * Floating content panel triggered by click.
 *
 * Slots:
 * - content: Popover content container
 */
export const popoverConfig = {
  slots: {
    /**
     * content: Popover panel
     *
     * Common styles:
     * - Background (bg-background, bg-surface-1)
     * - Border (border, border-border)
     * - Border radius (rounded-lg, rounded-xl)
     * - Shadow (shadow-lg)
     */
    content: '',
  },
};
