/**
 * ToggleGroup Component Theme Config
 *
 * Group of mutually exclusive toggles.
 *
 * Slots:
 * - base: Root container
 * - item: Individual toggle in group
 */
export const toggleGroupConfig = {
  slots: {
    base: '',
    item: '',
  },
};
