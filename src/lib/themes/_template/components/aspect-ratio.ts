/**
 * AspectRatio Component Theme Config
 *
 * Container that maintains a specific aspect ratio.
 *
 * Slots:
 * - base: Container element
 */
export const aspectRatioConfig = {
  slots: {
    /**
     * base: Aspect ratio container
     *
     * Common styles:
     * - Border radius (rounded-lg)
     * - Border (border, border-border)
     * - Background (bg-surface-1)
     * - Overflow (overflow-hidden)
     */
    base: [
      // TODO: 'rounded-lg', 'overflow-hidden'
    ],
  },
};
