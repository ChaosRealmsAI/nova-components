/**
 * Command Component Theme Config
 *
 * Command palette (search + actions).
 *
 * Slots:
 * - base: Root container
 * - input: Search input
 * - list: Results list
 * - group, separator, item, shortcut: List elements
 */
export const commandConfig = {
  slots: {
    base: '',
    input: '',
    list: '',
    empty: '',
    group: '',
    separator: '',
    item: '',
    shortcut: '',
  },
};
