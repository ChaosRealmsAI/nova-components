/**
 * HoverCard Component Theme Config
 *
 * Floating card on hover (preview cards).
 *
 * Slots:
 * - content: Card container
 */
export const hoverCardConfig = {
  slots: {
    content: '',
  },
};
