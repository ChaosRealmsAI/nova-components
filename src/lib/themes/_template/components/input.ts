/**
 * Input Component Theme Config
 *
 * Text input field for user data entry.
 *
 * Slots:
 * - base: Root <input> element styles
 */
export const inputConfig = {
  slots: {
    /**
     * base: Root input element
     *
     * Common styles:
     * - Background (bg-surface-1, bg-background)
     * - Border (border, border-border)
     * - Text color (text-foreground)
     * - Border radius (rounded-md, rounded-lg)
     * - Focus states (focus-visible:ring-*, focus-visible:border-*)
     * - Placeholder (placeholder:text-muted-foreground)
     */
    base: [
      // TODO: Add your input styles
      // Example: 'bg-surface-1', 'border-border', 'rounded-md'
      // Example: 'focus-visible:ring-primary'
    ],
  },
};
