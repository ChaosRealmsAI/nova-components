/**
 * Button 组件样式
 *
 * ═══════════════════════════════════════════════════════════════════════════════
 * ⭐ 核心：Slot 级别的主题定制，不是简单换色
 * ═══════════════════════════════════════════════════════════════════════════════
 *
 * 你拥有的能力（必须充分发挥）：
 *
 * │ 维度   │ 你可以做什么                                                    │
 * │────────│─────────────────────────────────────────────────────────────────│
 * │ 形状   │ 圆角大小、切角、不规则形状、clip-path 任意裁剪                    │
 * │ 字体   │ 字族、大小写、字重、字距、行高                                   │
 * │ 动效   │ hover 变化、active 反馈、过渡时长、动画曲线                      │
 * │ 阴影   │ 无阴影、柔和、硬边、发光、内凹、多层                             │
 * │ 边框   │ 粗细、样式、颜色、发光、渐变                                     │
 * │ 背景   │ 纯色、渐变、半透明、模糊、图案                                   │
 *
 *

 * 🎨 颜色语义：
 *    - default: bg-primary（主要操作，最强视觉权重）
 *    - secondary: bg-secondary（次要操作）
 *    - destructive: bg-destructive（危险操作，红色系）
 *    - outline/ghost: 透明背景，hover 时显示
 *    - 文字必须与背景形成足够对比
 *
 * ═══════════════════════════════════════════════════════════════════════════════
 * 开发原则
 * ═══════════════════════════════════════════════════════════════════════════════
 *
 * 1. 设计语言驱动
 *    先读 DESIGN.md，理解主题的设计故事和设计语言
 *    每个样式决策都要问：这符合设计语言吗？
 *
 * 2. 差异化检验
 *    与默认主题对比，必须有明显视觉差异
 *    如果只是换了颜色，说明没有发挥 Slot 能力
 *
 * 3. 可读性底线
 *    文字与背景对比度 ≥ 4.5:1
 *    眯眼测试：看不清就是对比度不够
 *
 * 4. 状态清晰
 *    hover/active/focus/disabled 必须有明显视觉反馈
 *
 * 5. Token 规范
 *    颜色用 Token 类名：bg-primary, text-foreground
 *    阴影用 var()：shadow-[var(--shadow-md)]
 *
 * ═══════════════════════════════════════════════════════════════════════════════
 */
export const buttonConfig = {
  slots: {
    /**
     * base: 按钮基础样式
     * ─────────────────────────────────────────────────────────────────────
     * 应用于所有按钮，所有变体继承这些样式
     *
     * 自由发挥：
     * - 形状（圆角、切角、不规则...）
     * - 字体（字族、大小写、字重、字距...）
     * - 过渡动画
     * - 内边距
     * - 边框
     * - 阴影
     * - 任何符合主题风格的样式
     */
    base: [
      'inline-flex items-center justify-center whitespace-nowrap rounded-full text-sm font-medium ring-offset-background transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50',
      // Glass effect base
      'backdrop-blur-sm',
    ],
  },

  variants: {
    variant: {
      /**
       * default: 主要按钮 - Neon Glow & Glass
       */
      default: {
        base: [
          'bg-primary text-primary-foreground',
          'border border-white/10',
          // Glow effect
          'shadow-[0_0_15px_rgba(34,211,238,0.4)]', 
          // Hover: Brighter glow + slight lift
          'hover:shadow-[0_0_25px_rgba(34,211,238,0.6)] hover:-translate-y-0.5 hover:bg-primary/90',
          // Active: Press down
          'active:scale-95 active:shadow-[0_0_5px_rgba(34,211,238,0.4)]',
        ],
      },

      /**
       * destructive: 危险按钮 - Red Glow
       */
      destructive: {
        base: [
          'bg-destructive text-destructive-foreground',
          'border border-red-500/30',
          'shadow-[0_0_15px_rgba(239,68,68,0.4)]',
          'hover:bg-destructive/90 hover:shadow-[0_0_25px_rgba(239,68,68,0.6)] hover:-translate-y-0.5',
          'active:scale-95',
        ],
      },

      /**
       * outline: 轮廓按钮 - Glass Border
       */
      outline: {
        base: [
          'border border-white/20 bg-white/5',
          'text-foreground',
          'hover:bg-white/10 hover:border-white/40 hover:text-accent-foreground',
          'hover:shadow-[0_0_10px_rgba(255,255,255,0.1)]',
          'active:scale-95',
        ],
      },

      /**
       * secondary: 次要按钮 - Frosted Glass
       */
      secondary: {
        base: [
          'bg-secondary text-secondary-foreground',
          'border border-white/10',
          'hover:bg-secondary/80 hover:-translate-y-0.5',
          'active:scale-95',
        ],
      },

      /**
       * ghost: 幽灵按钮 - Minimal
       */
      ghost: {
        base: [
          'hover:bg-white/10 hover:text-accent-foreground',
          'active:scale-95',
        ],
      },

      /**
       * link: 链接按钮
       */
      link: {
        base: [
          'text-primary underline-offset-4 hover:underline',
          'shadow-none hover:shadow-none', // Remove glow for link
        ],
      },
    },
  },
};
