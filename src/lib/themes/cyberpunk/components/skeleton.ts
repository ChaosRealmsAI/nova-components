/**
 * Skeleton 组件样式 - Cyberpunk (Loading State)
 */
export const skeletonConfig = {
  slots: {
    base: [ // Changed from root to base
      'animate-pulse rounded-none bg-muted',
      'bg-primary/10',
      // Glitchy animation overlay could be added here if CSS keyframes were available globally
    ],
  },
};
