/**
 * Pagination 组件样式
 *
 * ═══════════════════════════════════════════════════════════════════════════════
 * ⭐ 核心：Slot 级别的主题定制，不是简单换色
 * ═══════════════════════════════════════════════════════════════════════════════
 *
 * 你拥有的能力（必须充分发挥）：
 *
 * │ 维度   │ 你可以做什么                                                    │
 * │────────│─────────────────────────────────────────────────────────────────│
 * │ 形状   │ 圆角大小、切角、不规则形状、clip-path 任意裁剪                    │
 * │ 字体   │ 字族、大小写、字重、字距、行高                                   │
 * │ 动效   │ hover 变化、active 反馈、过渡时长、动画曲线                      │
 * │ 阴影   │ 无阴影、柔和、硬边、发光、内凹、多层                             │
 * │ 边框   │ 粗细、样式、颜色、发光、渐变                                     │
 * │ 背景   │ 纯色、渐变、半透明、模糊、图案                                   │
 *
 *

 * 🎨 颜色语义：
 *    - 普通页码：text-foreground
 *    - 当前页：bg-primary text-primary-foreground
 *    - 禁用：text-muted-foreground opacity-50
 *
 * ═══════════════════════════════════════════════════════════════════════════════
 * 开发原则
 * ═══════════════════════════════════════════════════════════════════════════════
 *
 * 1. 设计语言驱动
 *    先读 DESIGN.md，理解主题的设计故事和设计语言
 *    每个样式决策都要问：这符合设计语言吗？
 *
 * 2. 差异化检验
 *    与默认主题对比，必须有明显视觉差异
 *    如果只是换了颜色，说明没有发挥 Slot 能力
 *
 * 3. 可读性底线
 *    文字与背景对比度 ≥ 4.5:1
 *    眯眼测试：看不清就是对比度不够
 *
 * 4. 状态清晰
 *    hover/active/focus/disabled 必须有明显视觉反馈
 *
 * 5. Token 规范
 *    颜色用 Token 类名：bg-primary, text-foreground
 *    阴影用 var()：shadow-[var(--shadow-md)]
 *
 * ═══════════════════════════════════════════════════════════════════════════════
 */
export const paginationConfig = {
  slots: {
    /**
     * root: 分页根容器
     * ─────────────────────────────────────────────────────────────────────
     * 整个分页组件的容器
     */
    root: 'mx-auto flex w-full justify-center',

    /**
     * content: 分页内容
     * ─────────────────────────────────────────────────────────────────────
     * 包裹所有分页项的容器
     */
    content: 'flex flex-row items-center gap-1',

    /**
     * item: 分页项
     * ─────────────────────────────────────────────────────────────────────
     * 单个分页项的容器
     */
    item: 'inline-flex items-center',

    /**
     * link: 分页链接
     * ─────────────────────────────────────────────────────────────────────
     * 可点击的分页链接
     *
     * 自由发挥：
     * - 形状
     * - 边框
     * - hover 效果
     * - 激活状态 (data-[active=true]) 样式
     */
    link: [
      'inline-flex items-center justify-center',
      'h-9 min-w-9 px-4',
      'text-sm font-medium text-foreground',
      // Glass background on hover
      'bg-transparent',
      'hover:bg-white/[0.08] hover:backdrop-blur-md',
      // Glass border
      'border border-transparent',
      'hover:border-white/[0.15]',
      // Shape
      'rounded-lg',
      // Transition
      'transition-all duration-200',
      // Active state - glass with glow
      'data-[active=true]:bg-white/[0.12] data-[active=true]:backdrop-blur-xl',
      'data-[active=true]:border-white/[0.2]',
      'data-[active=true]:shadow-[0_0_12px_rgba(139,92,246,0.4)]',
      'data-[active=true]:text-primary',
      // Disabled state
      'disabled:pointer-events-none disabled:opacity-50',
    ],

    /**
     * ellipsis: 省略号
     * ─────────────────────────────────────────────────────────────────────
     * 分页中的省略号
     */
    ellipsis: 'flex h-9 w-9 items-center justify-center text-muted-foreground',
  },

  variants: {
    variant: {
      /**
       * default: 默认分页
       */
      default: {
        root: 'flex w-full justify-center',
        content: 'flex items-center gap-1',
        item: 'inline-flex',
        link: 'rounded-lg transition-colors',
      },

      /**
       * outline: 轮廓分页
       * ───────────────────────────────────────────────────────────────────
       * 使用轮廓边框的分页样式
       */
      outline: {
        link: [
          'border-white/[0.15]',
          'hover:border-white/[0.25]',
        ].join(' '),
      },
    },

    size: {
      default: {
        link: 'h-9 min-w-9 px-4 text-sm',
      },
      sm: {
        link: 'h-8 min-w-8 px-3 text-xs',
      },
      lg: {
        link: 'h-10 min-w-10 px-5 text-base',
      },
      icon: {
        link: 'h-9 w-9 p-0',
      },
    },
  },
};
