/**
 * Button 组件样式
 *
 * ═══════════════════════════════════════════════════════════════════════════════
 * ⭐ 核心：Slot 级别的主题定制，不是简单换色
 * ═══════════════════════════════════════════════════════════════════════════════
 *
 * 你拥有的能力（必须充分发挥）：
 *
 * │ 维度   │ 你可以做什么                                                    │
 * │────────│─────────────────────────────────────────────────────────────────│
 * │ 形状   │ 圆角大小、切角、不规则形状、clip-path 任意裁剪                    │
 * │ 字体   │ 字族、大小写、字重、字距、行高                                   │
 * │ 动效   │ hover 变化、active 反馈、过渡时长、动画曲线                      │
 * │ 阴影   │ 无阴影、柔和、硬边、发光、内凹、多层                             │
 * │ 边框   │ 粗细、样式、颜色、发光、渐变                                     │
 * │ 背景   │ 纯色、渐变、半透明、模糊、图案                                   │
 *
 *

 * 🎨 颜色语义：
 *    - default: bg-primary（主要操作，最强视觉权重）
 *    - secondary: bg-secondary（次要操作）
 *    - destructive: bg-destructive（危险操作，红色系）
 *    - outline/ghost: 透明背景，hover 时显示
 *    - 文字必须与背景形成足够对比
 *
 * ═══════════════════════════════════════════════════════════════════════════════
 * 开发原则
 * ═══════════════════════════════════════════════════════════════════════════════
 *
 * 1. 设计语言驱动
 *    先读 DESIGN.md，理解主题的设计故事和设计语言
 *    每个样式决策都要问：这符合设计语言吗？
 *
 * 2. 差异化检验
 *    与默认主题对比，必须有明显视觉差异
 *    如果只是换了颜色，说明没有发挥 Slot 能力
 *
 * 3. 可读性底线
 *    文字与背景对比度 ≥ 4.5:1
 *    眯眼测试：看不清就是对比度不够
 *
 * 4. 状态清晰
 *    hover/active/focus/disabled 必须有明显视觉反馈
 *
 * 5. Token 规范
 *    颜色用 Token 类名：bg-primary, text-foreground
 *    阴影用 var()：shadow-[var(--shadow-md)]
 *
 * ═══════════════════════════════════════════════════════════════════════════════
 */
export const buttonConfig = {
  slots: {
    /**
     * base: 按钮基础样式
     * ─────────────────────────────────────────────────────────────────────
     * 应用于所有按钮，所有变体继承这些样式
     *
     * 自由发挥：
     * - 形状（圆角、切角、不规则...）
     * - 字体（字族、大小写、字重、字距...）
     * - 过渡动画
     * - 内边距
     * - 边框
     * - 阴影
     * - 任何符合主题风格的样式
     */
    base: [
      // TODO: 根据主题风格自由发挥
    ],
  },

  variants: {
    variant: {
      /**
       * default: 主要按钮
       * ───────────────────────────────────────────────────────────────────
       * 用途：主要操作，最强视觉权重
       * 约束：必须使用 primary Token 作为主色
       *
       * 自由发挥：hover效果、阴影、发光、渐变、动画、位移、缩放...
       */
      default: {
        base: [
          // TODO
        ],
      },

      /**
       * destructive: 危险按钮
       * ───────────────────────────────────────────────────────────────────
       * 用途：删除、移除等破坏性操作
       * 约束：必须使用 destructive Token 作为主色
       *
       * 自由发挥：警示效果（条纹、脉冲、强烈阴影...）
       */
      destructive: {
        base: [
          // TODO
        ],
      },

      /**
       * outline: 轮廓按钮
       * ───────────────────────────────────────────────────────────────────
       * 用途：次要操作
       * 约束：透明/半透明背景 + 可见边界（边框/ring/inset shadow 均可）
       *
       * 自由发挥：边界样式、hover 变化、发光效果...
       */
      outline: {
        base: [
          // TODO
        ],
      },

      /**
       * secondary: 次要按钮
       * ───────────────────────────────────────────────────────────────────
       * 用途：辅助操作，视觉权重弱于 default
       * 约束：必须使用 secondary Token 作为主色
       *
       * 自由发挥：任何符合主题的样式
       */
      secondary: {
        base: [
          // TODO
        ],
      },

      /**
       * ghost: 幽灵按钮
       * ───────────────────────────────────────────────────────────────────
       * 用途：最弱视觉权重，常用于工具栏
       * 约束：默认透明背景，hover 时才显示背景
       *
       * 自由发挥：hover 背景样式、微弱发光...
       */
      ghost: {
        base: [
          // TODO
        ],
      },

      /**
       * link: 链接按钮
       * ───────────────────────────────────────────────────────────────────
       * 用途：看起来像文字链接
       * 约束：无背景无边框，具有链接特征
       *
       * 自由发挥：下划线样式、hover 效果...
       */
      link: {
        base: [
          // TODO
        ],
      },
    },
  },
};
