/**
 * Badge 组件样式模板
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * 开发原则：
 *
 * 1. Token 优先
 *    - 有 Token 的地方必须使用 Token（颜色、阴影、圆角、字体等）
 *    - 开发组件样式前，先完成 tokens.ts 的定义
 *    - 语法：颜色用类名(bg-primary)，阴影用 var()(shadow-[var(--shadow-md)])
 *
 * 2. 结构固定，样式灵活
 *    - slots 和 variants 的 key 必须保持一致（系统依赖）
 *    - value 完全自由，根据主题风格发挥创意
 *
 * 3. 风格统一
 *    - 同一主题内所有组件应保持一致的设计语言
 *    - 如：圆角风格、阴影风格、动画风格等
 *
 * 4. 只需满足用途约束
 *    - 每个 variant 有其用途约束（如 destructive 必须用 destructive Token）
 *    - 只要满足约束，实现方式完全自由
 * ═══════════════════════════════════════════════════════════════════════════
 */
export const badgeConfig = {
  slots: {
    /**
     * base: 徽章基础样式
     * ─────────────────────────────────────────────────────────────────────
     * 应用于所有徽章，所有变体继承这些样式
     *
     * 自由发挥：
     * - 形状（圆角、药丸形、切角...）
     * - 字体（大小、字重、大小写、字距...）
     * - 内边距
     * - 边框
     * - 任何符合主题风格的样式
     */
    base: [
      // TODO: 根据主题风格自由发挥
    ],
  },

  variants: {
    variant: {
      /**
       * default: 默认徽章
       * ───────────────────────────────────────────────────────────────────
       * 用途：通用信息展示
       * 约束：必须使用 primary Token 作为主色
       *
       * 自由发挥：背景样式、发光效果、阴影...
       */
      default: {
        base: [
          // TODO
        ],
      },

      /**
       * secondary: 次要徽章
       * ───────────────────────────────────────────────────────────────────
       * 用途：辅助信息
       * 约束：必须使用 secondary Token 作为主色
       *
       * 自由发挥：任何符合主题的样式
       */
      secondary: {
        base: [
          // TODO
        ],
      },

      /**
       * destructive: 危险徽章
       * ───────────────────────────────────────────────────────────────────
       * 用途：错误、警告、删除状态
       * 约束：必须使用 destructive Token 作为主色
       *
       * 自由发挥：警示效果...
       */
      destructive: {
        base: [
          // TODO
        ],
      },

      /**
       * outline: 轮廓徽章
       * ───────────────────────────────────────────────────────────────────
       * 用途：轻量级标签
       * 约束：透明/半透明背景 + 可见边界
       *
       * 自由发挥：边框样式、发光效果...
       */
      outline: {
        base: [
          // TODO
        ],
      },
    },
  },
};
