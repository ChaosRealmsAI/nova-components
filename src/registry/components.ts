/**
 * Component Registry - Re-export
 *
 * 此文件保持向后兼容，所有内容已迁移到 components/ 目录
 *
 * 目录结构:
 * - components/types.ts      - 类型定义
 * - components/atoms/        - 原子组件 entries (每个组件一个文件)
 * - components/blocks/       - Block 组件 entries (每个组件一个文件)
 * - components/index.ts      - 聚合导出
 */

export * from './components/index';
